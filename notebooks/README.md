# Historical notebooks

These notebooks are retained as the experimental record supplied by the authors. The maintained execution path is `scripts/run_pso_legacy.py` together with the configuration in `.env`. Notebook outputs may be large and may reflect experiment-specific filenames.
