# Security

Remote-scripting passwords must be stored only in `.env`. A password was embedded in the originally supplied research script and has been removed from this repository package. Rotate that password before using or publishing the repository.

Report accidental credential exposure by immediately removing the secret from the repository history and rotating it in PLAXIS.
